diff --git a/cmake/dependencies/common.cmake b/cmake/dependencies/common.cmake
index 8f5813ac..0dbf8c3a 100644
--- a/cmake/dependencies/common.cmake
+++ b/cmake/dependencies/common.cmake
@@ -9,7 +9,7 @@ set(imgui_apply_patch_command ${CMAKE_COMMAND} -Dpatch_file=${imgui_fixes_and_co
 FetchContent_Declare(
     ImGui
     GIT_REPOSITORY https://github.com/ocornut/imgui.git
-    GIT_TAG v1.91.9b-docking
+    GIT_TAG 4806a1924ff6181180bf5e4b8b79ab4394118875 # v1.91.9b-docking
     PATCH_COMMAND ${imgui_apply_patch_command}
 )
 FetchContent_MakeAvailable(ImGui)
@@ -43,7 +43,7 @@ if(INCLUDE_MPQ_SUPPORT)
     FetchContent_Declare(
         StormLib
         GIT_REPOSITORY https://github.com/ladislav-zezula/StormLib.git
-        GIT_TAG v9.25
+        GIT_TAG 28c9b4be3f23c6b3a5ff55cacac7dbe5b9cdc4fc # v9.25
         PATCH_COMMAND ${stormlib_apply_patch_command}
     )
     FetchContent_MakeAvailable(StormLib)
@@ -53,4 +53,9 @@ endif()
 #=================== STB ===================
 set(STB_DIR ${CMAKE_BINARY_DIR}/_deps/stb)
-file(DOWNLOAD "https://github.com/nothings/stb/raw/0bc88af4de5fb022db643c2d8e549a0927749354/stb_image.h" "${STB_DIR}/stb_image.h")
+file(
+    DOWNLOAD
+        "https://github.com/nothings/stb/raw/0bc88af4de5fb022db643c2d8e549a0927749354/stb_image.h"
+        "${STB_DIR}/stb_image.h"
+    EXPECTED_HASH SHA256=c54b15a689e6a1f32c75e2ec23afa442e3e0e37e894b73c1974d08679b20dd5c
+)
 file(WRITE "${STB_DIR}/stb_impl.c" "#define STB_IMAGE_IMPLEMENTATION\n#include \"stb_image.h\"")
@@ -97,6 +102,6 @@ endif()
 FetchContent_Declare(
     ThreadPool
     GIT_REPOSITORY https://github.com/bshoshany/thread-pool.git
-    GIT_TAG v4.1.0
+    GIT_TAG 097aa718f25d44315cadb80b407144ad455ee4f9 # v4.1.0
 )
 FetchContent_MakeAvailable(ThreadPool)
@@ -110,4 +115,4 @@ FetchContent_Declare(
     GIT_REPOSITORY https://github.com/KiritoDv/prism-processor.git
     GIT_TAG bbcbc7e3f890a5806b579361e7aa0336acd547e7
 )
-FetchContent_MakeAvailable(prism)
\ No newline at end of file
+FetchContent_MakeAvailable(prism)
diff --git a/cmake/dependencies/ios.cmake b/cmake/dependencies/ios.cmake
index 2ed98be1..aad5c586 100644
--- a/cmake/dependencies/ios.cmake
+++ b/cmake/dependencies/ios.cmake
@@ -6,7 +6,7 @@ if (NOT ${SDL2_FOUND})
     FetchContent_Declare(
         SDL2
         GIT_REPOSITORY https://github.com/libsdl-org/SDL.git
-        GIT_TAG release-2.28.1
+        GIT_TAG 5d249570393f7a37e037abf22cd6012a4cc56a71 # release-2.32.10
         OVERRIDE_FIND_PACKAGE
     )
     FetchContent_MakeAvailable(SDL2)
@@ -18,7 +18,7 @@ if (NOT ${nlohmann_json_FOUND})
     FetchContent_Declare(
         nlohmann_json
         GIT_REPOSITORY https://github.com/nlohmann/json.git
-        GIT_TAG v3.11.3
+        GIT_TAG 55f93686c01528224f448c19128836e7df245f72 # v3.12.0
         OVERRIDE_FIND_PACKAGE
     )
     FetchContent_MakeAvailable(nlohmann_json)
@@ -31,7 +31,7 @@ if (NOT ${tinyxml2_FOUND})
     FetchContent_Declare(
         tinyxml2
         GIT_REPOSITORY https://github.com/leethomason/tinyxml2.git
-        GIT_TAG 10.0.0
+        GIT_TAG 9148bdf719e997d1f474be6bcc7943881046dba1 # 11.0.0
         OVERRIDE_FIND_PACKAGE
     )
     FetchContent_MakeAvailable(tinyxml2)
@@ -43,7 +43,7 @@ if (NOT ${spdlog_FOUND})
     FetchContent_Declare(
         spdlog
         GIT_REPOSITORY https://github.com/gabime/spdlog.git
-        GIT_TAG v1.14.1
+        GIT_TAG 486b55554f11c9cccc913e11a87085b2a91f706f # v1.16.0
         OVERRIDE_FIND_PACKAGE
     )
     FetchContent_MakeAvailable(spdlog)
@@ -62,7 +62,7 @@ if (NOT ${libzip_FOUND})
     FetchContent_Declare(
         libzip
         GIT_REPOSITORY https://github.com/nih-at/libzip.git
-        GIT_TAG v1.10.1
+        GIT_TAG 6f8a0cdd24a0dc6cce9dac4a7679da784ab124ea # v1.11.4
         OVERRIDE_FIND_PACKAGE
     )
     FetchContent_MakeAvailable(libzip)
@@ -73,7 +73,7 @@ endif()
 FetchContent_Declare(
     metalcpp
     GIT_REPOSITORY https://github.com/briaguya-ai/single-header-metal-cpp.git
-    GIT_TAG macOS13_iOS16
+    GIT_TAG 7bf710a725b386f6db869f8a2b48a56be29b6233 # macOS13_iOS16
 )
 FetchContent_MakeAvailable(metalcpp)
 list(APPEND ADDITIONAL_LIB_INCLUDES ${metalcpp_SOURCE_DIR})
diff --git a/cmake/dependencies/mac.cmake b/cmake/dependencies/mac.cmake
index 1a1694d3..31479f12 100644
--- a/cmake/dependencies/mac.cmake
+++ b/cmake/dependencies/mac.cmake
@@ -8,7 +8,7 @@ if (NOT ${spdlog_FOUND})
     FetchContent_Declare(
         spdlog
         GIT_REPOSITORY https://github.com/gabime/spdlog.git
-        GIT_TAG v1.14.1
+        GIT_TAG 6fa36017cfd5731d617e1a934f0e5ea9c4445b13 # v1.15.3
         OVERRIDE_FIND_PACKAGE
     )
     FetchContent_MakeAvailable(spdlog)
@@ -18,7 +18,7 @@ endif()
 FetchContent_Declare(
     metalcpp
     GIT_REPOSITORY https://github.com/briaguya-ai/single-header-metal-cpp.git
-    GIT_TAG macOS13_iOS16
+    GIT_TAG 7bf710a725b386f6db869f8a2b48a56be29b6233 # macOS13_iOS16
 )
 FetchContent_MakeAvailable(metalcpp)
 list(APPEND ADDITIONAL_LIB_INCLUDES ${metalcpp_SOURCE_DIR})
diff --git a/cmake/ios-toolchain-populate.cmake b/cmake/ios-toolchain-populate.cmake
index eb93fea4..644bcc26 100644
--- a/cmake/ios-toolchain-populate.cmake
+++ b/cmake/ios-toolchain-populate.cmake
@@ -1,4 +1,6 @@
-set(PLATFORM "OS64COMBINED")
+if(NOT DEFINED PLATFORM)
+    set(PLATFORM "OS64COMBINED")
+endif()
 include(FetchContent)
 FetchContent_Declare(iostoolchain
     GIT_REPOSITORY https://github.com/leetal/ios-cmake
@@ -12,4 +14,4 @@ if(NOT iostoolchain_POPULATED)
 endif()
 set(CMAKE_IOS_TOOLCHAIN_FILE ${iostoolchain_SOURCE_DIR}/ios.toolchain.cmake)
 set_property(GLOBAL PROPERTY IOS_TOOLCHAIN_FILE ${CMAKE_IOS_TOOLCHAIN_FILE})
-include(${CMAKE_IOS_TOOLCHAIN_FILE})
\ No newline at end of file
+include(${CMAKE_IOS_TOOLCHAIN_FILE})
diff --git a/include/fast/backends/gfx_metal.h b/include/fast/backends/gfx_metal.h
index b926895a..13961899 100644
--- a/include/fast/backends/gfx_metal.h
+++ b/include/fast/backends/gfx_metal.h
@@ -29,6 +29,7 @@ class Device;
 class Function;
 class Buffer;
 class RenderPipelineState;
+class DepthStencilState;
 class CommandQueue;
 class Viewport;
 } // namespace MTL
@@ -116,7 +117,7 @@ struct CoordUniforms {

 class GfxRenderingAPIMetal final : public GfxRenderingAPI {
   public:
-    ~GfxRenderingAPIMetal() override = default;
+    ~GfxRenderingAPIMetal() override;
     const char* GetName() override;
     int GetMaxTextureSize() override;
     GfxClipParameters GetClipParameters() override;
@@ -184,6 +185,7 @@ class GfxRenderingAPIMetal final : public GfxRenderingAPI {
     FrameUniforms mFrameUniforms;
     CoordUniforms mCoordUniforms;
     MTL::Buffer* mFrameUniformBuffer;
+    MTL::DepthStencilState* mDepthStencilStates[2][2][2] = {};

     uint32_t mMsaaNumQualityLevels[METAL_MAX_MULTISAMPLE_SAMPLE_COUNT];

diff --git a/include/fast/backends/gfx_sdl.h b/include/fast/backends/gfx_sdl.h
index 1f03cb63..27d6a9b6 100644
--- a/include/fast/backends/gfx_sdl.h
+++ b/include/fast/backends/gfx_sdl.h
@@ -61,5 +61,8 @@ class GfxWindowBackendSDL2 final : public GfxWindowBackend {
     int mWindowWidth = 640;
     int mWindowHeight = 480;
     void (*mOnAllKeysUp)();
+#ifdef __IOS__
+    bool mIsBackgrounded = false;
+#endif
 };
 } // namespace Fast
diff --git a/include/libultraship/bridge/windowbridge.h b/include/libultraship/bridge/windowbridge.h
index 623f16d3..5be84e59 100644
--- a/include/libultraship/bridge/windowbridge.h
+++ b/include/libultraship/bridge/windowbridge.h
@@ -7,6 +7,7 @@ extern "C" {
 #endif

 bool WindowIsRunning();
+void WindowWaitForFrameReady();
 uint32_t WindowGetWidth();
 uint32_t WindowGetHeight();
 float WindowGetAspectRatio();
diff --git a/include/ship/audio/Audio.h b/include/ship/audio/Audio.h
index 3190664c..a842991c 100644
--- a/include/ship/audio/Audio.h
+++ b/include/ship/audio/Audio.h
@@ -19,6 +19,7 @@ class Audio {
     AudioBackend GetCurrentAudioBackend();
     std::shared_ptr<std::vector<AudioBackend>> GetAvailableAudioBackends();
     void SetCurrentAudioBackend(AudioBackend backend);
+    void SetPaused(bool paused);

   protected:
     void InitAudioPlayer();
diff --git a/include/ship/audio/AudioPlayer.h b/include/ship/audio/AudioPlayer.h
index 1d8b1c47..40b05b40 100644
--- a/include/ship/audio/AudioPlayer.h
+++ b/include/ship/audio/AudioPlayer.h
@@ -23,6 +23,8 @@ class AudioPlayer {
     bool Init();
     virtual int32_t Buffered() = 0;
     virtual void Play(const uint8_t* buf, size_t len) = 0;
+    virtual void SetPaused(bool) {
+    }

     bool IsInitialized();

diff --git a/include/ship/audio/SDLAudioPlayer.h b/include/ship/audio/SDLAudioPlayer.h
index fb82faf9..9998c82f 100644
--- a/include/ship/audio/SDLAudioPlayer.h
+++ b/include/ship/audio/SDLAudioPlayer.h
@@ -1,5 +1,6 @@
 #pragma once
 #include "AudioPlayer.h"
+#include <atomic>
 #include <SDL2/SDL.h>

 namespace Ship {
@@ -9,14 +10,16 @@ class SDLAudioPlayer final : public AudioPlayer {
     }
     ~SDLAudioPlayer();

-    int Buffered();
-    void Play(const uint8_t* buf, size_t len);
+    int Buffered() override;
+    void Play(const uint8_t* buf, size_t len) override;
+    void SetPaused(bool paused) override;

   protected:
-    bool DoInit();
+    bool DoInit() override;

   private:
     SDL_AudioDeviceID mDevice;
     int32_t mNumChannels = 2;
+    std::atomic_bool mPaused = false;
 };
 } // namespace Ship
diff --git a/src/fast/Fast3dWindow.cpp b/src/fast/Fast3dWindow.cpp
index 9943170d..6c4cba83 100644
--- a/src/fast/Fast3dWindow.cpp
+++ b/src/fast/Fast3dWindow.cpp
@@ -76,6 +76,10 @@ void Fast3dWindow::Init() {
     int32_t posX, posY;

     isFullscreen = Ship::Context::GetInstance()->GetConfig()->GetBool("Window.Fullscreen.Enabled", false) || gameMode;
+#ifdef __IOS__
+    isFullscreen = false;
+    Ship::Context::GetInstance()->GetConfig()->SetBool("Window.Fullscreen.Enabled", false);
+#endif
     posX = Ship::Context::GetInstance()->GetConfig()->GetInt("Window.PositionX", 100);
     posY = Ship::Context::GetInstance()->GetConfig()->GetInt("Window.PositionY", 100);

diff --git a/src/fast/backends/gfx_metal.cpp b/src/fast/backends/gfx_metal.cpp
index ad7cc49d..11e3d531 100644
--- a/src/fast/backends/gfx_metal.cpp
+++ b/src/fast/backends/gfx_metal.cpp
@@ -43,6 +43,18 @@
 // MARK: - Helpers
 namespace Fast {

+GfxRenderingAPIMetal::~GfxRenderingAPIMetal() {
+    for (auto& depthTestStates : mDepthStencilStates) {
+        for (auto& depthMaskStates : depthTestStates) {
+            for (MTL::DepthStencilState* state : depthMaskStates) {
+                if (state != nullptr) {
+                    state->release();
+                }
+            }
+        }
+    }
+}
+
 static MTL::SamplerAddressMode gfx_cm_to_metal(uint32_t val) {
     switch (val) {
         case G_TX_NOMIRROR | G_TX_CLAMP:
@@ -418,20 +430,23 @@ void GfxRenderingAPIMetal::DrawTriangles(float buf_vbo[], size_t buf_vbo_len, si
     auto& current_framebuffer = mFramebuffers[mCurrentFramebuffer];

     if (current_framebuffer.mLastDepthTest != mCurrentDepthTest ||
-        current_framebuffer.mLastDepthMask != mCurrentDepthMask) {
+        current_framebuffer.mLastDepthMask != mCurrentDepthMask ||
+        current_framebuffer.mLastZmodeDecal != mCurrentZmodeDecal) {
         current_framebuffer.mLastDepthTest = mCurrentDepthTest;
         current_framebuffer.mLastDepthMask = mCurrentDepthMask;

-        MTL::DepthStencilDescriptor* depth_descriptor = MTL::DepthStencilDescriptor::alloc()->init();
-        depth_descriptor->setDepthWriteEnabled(mCurrentDepthMask);
-        depth_descriptor->setDepthCompareFunction(
-            mCurrentDepthTest ? (mCurrentZmodeDecal ? MTL::CompareFunctionLessEqual : MTL::CompareFunctionLess)
-                              : MTL::CompareFunctionAlways);
-
-        MTL::DepthStencilState* depth_stencil_state = mDevice->newDepthStencilState(depth_descriptor);
+        MTL::DepthStencilState*& depth_stencil_state =
+            mDepthStencilStates[mCurrentDepthTest][mCurrentDepthMask][mCurrentZmodeDecal];
+        if (depth_stencil_state == nullptr) {
+            MTL::DepthStencilDescriptor* depth_descriptor = MTL::DepthStencilDescriptor::alloc()->init();
+            depth_descriptor->setDepthWriteEnabled(mCurrentDepthMask);
+            depth_descriptor->setDepthCompareFunction(
+                mCurrentDepthTest ? (mCurrentZmodeDecal ? MTL::CompareFunctionLessEqual : MTL::CompareFunctionLess)
+                                  : MTL::CompareFunctionAlways);
+            depth_stencil_state = mDevice->newDepthStencilState(depth_descriptor);
+            depth_descriptor->release();
+        }
         current_framebuffer.mCommandEncoder->setDepthStencilState(depth_stencil_state);
-
-        depth_descriptor->release();
     }

     if (current_framebuffer.mLastZmodeDecal != mCurrentZmodeDecal) {
@@ -599,15 +614,17 @@ void GfxRenderingAPIMetal::SetupScreenFramebuffer(uint32_t width, uint32_t heigh
     mCurrentDrawable = nullptr;
     mCurrentDrawable = mLayer->nextDrawable();

-    bool msaa_enabled = Ship::Context::GetInstance()->GetConsoleVariables()->GetInteger("gMSAAValue", 1) > 1;
+    bool msaa_enabled = Ship::Context::GetInstance()->GetConsoleVariables()->GetInteger(CVAR_MSAA_VALUE, 1) > 1;

     FramebufferMetal& fb = mFramebuffers[0];
     TextureDataMetal& tex = mTextures[fb.mTextureId];

     NS::AutoreleasePool* autorelease_pool = NS::AutoreleasePool::alloc()->init();

-    if (tex.texture != nullptr)
-        tex.texture->release();
+    // The screen texture is owned by CAMetalDrawable. Releasing this borrowed
+    // pointer here over-releases it; QuartzCore releases it again when the
+    // drawable is retired, which crashes when iOS backgrounds the app.
+    // Assign the next drawable's texture and let the drawable own its lifetime.

     tex.texture = mCurrentDrawable->texture();

@@ -713,7 +730,7 @@ void GfxRenderingAPIMetal::UpdateFramebufferParameters(int fb_id, uint32_t width

             bool fb_msaa_enabled = (msaa_level > 1);
             bool game_msaa_enabled =
-                Ship::Context::GetInstance()->GetConsoleVariables()->GetInteger("gMSAAValue", 1) > 1;
+                Ship::Context::GetInstance()->GetConsoleVariables()->GetInteger(CVAR_MSAA_VALUE, 1) > 1;

             if (fb_msaa_enabled) {
                 render_pass_descriptor->colorAttachments()->object(0)->setTexture(tex.msaaTexture);
diff --git a/src/fast/backends/gfx_sdl2.cpp b/src/fast/backends/gfx_sdl2.cpp
index eb5df8b2..5a2b85a7 100644
--- a/src/fast/backends/gfx_sdl2.cpp
+++ b/src/fast/backends/gfx_sdl2.cpp
@@ -9,6 +9,11 @@
 #endif

 #include "ship/Context.h"
+#ifdef __IOS__
+#include "ship/audio/Audio.h"
+#include "ship/config/Config.h"
+#include "ship/window/Window.h"
+#endif
 #include "ship/config/ConsoleVariable.h"
 #include "ship/controller/controldeck/ControlDeck.h"
 #include "ship/window/FileDropMgr.h"
@@ -22,7 +27,9 @@
 #elif __APPLE__
 #include <SDL.h>
 #include "fast/backends/gfx_metal.h"
+#if !defined(__IOS__)
 #include "ship/utils/macUtils.h"
+#endif
 #else
 #include <SDL2/SDL.h>
 #define GL_GLEXT_PROTOTYPES 1
@@ -45,6 +52,25 @@ LONG_PTR SDL_WndProc;
 #endif

 namespace Fast {
+#ifdef __IOS__
+static void SaveIOSConfiguration() {
+    auto context = Ship::Context::GetInstance();
+    if (context->GetWindow() != nullptr) {
+        context->GetWindow()->SaveWindowToConfig();
+    }
+    if (context->GetConfig() != nullptr) {
+        context->GetConfig()->Save();
+    }
+}
+
+static void SetIOSAudioPaused(bool paused) {
+    auto audio = Ship::Context::GetInstance()->GetAudio();
+    if (audio != nullptr) {
+        audio->SetPaused(paused);
+    }
+}
+#endif
+
 const SDL_Scancode lus_to_sdl_table[] = {
     SDL_SCANCODE_UNKNOWN,
     SDL_SCANCODE_ESCAPE,
@@ -207,6 +233,16 @@ GfxWindowBackendSDL2::~GfxWindowBackendSDL2() {
 }

 void GfxWindowBackendSDL2::SetFullscreenImpl(bool on, bool call_callback) {
+#ifdef __IOS__
+    // UIKit owns the full-screen drawable. Desktop display-mode switching can
+    // detach the Metal surface on iOS.
+    mFullScreen = false;
+    if (mOnFullscreenChanged != nullptr && call_callback) {
+        mOnFullscreenChanged(false);
+    }
+    return;
+#endif
+
     if (mFullScreen == on) {
         return;
     }
@@ -227,7 +263,7 @@ void GfxWindowBackendSDL2::SetFullscreenImpl(bool on, bool call_callback) {
         }
     }

-#if defined(__APPLE__)
+#if defined(__APPLE__) && !defined(__IOS__)
     // Implement fullscreening with native macOS APIs
     if (on != isNativeMacOSFullscreenActive(mWnd)) {
         toggleNativeMacOSFullscreen(mWnd);
@@ -505,7 +541,7 @@ void GfxWindowBackendSDL2::SetMouseCallbacks(bool (*onMouseButtonDown)(int btn),
 }

 void GfxWindowBackendSDL2::GetDimensions(uint32_t* width, uint32_t* height, int32_t* posX, int32_t* posY) {
-#ifdef __APPLE__
+#if defined(__APPLE__) && !defined(__IOS__)
     SDL_GetWindowSize(mWnd, static_cast<int*>((void*)width), static_cast<int*>((void*)height));
 #else
     SDL_GL_GetDrawableSize(mWnd, static_cast<int*>((void*)width), static_cast<int*>((void*)height));
@@ -573,9 +609,19 @@ void GfxWindowBackendSDL2::HandleSingleEvent(SDL_Event& event) {
             OnKeyup(event.key.keysym.scancode);
             break;
         case SDL_MOUSEBUTTONDOWN:
+#ifdef __IOS__
+            if (event.button.which == SDL_TOUCH_MOUSEID) {
+                break;
+            }
+#endif
             OnMouseButtonDown(event.button.button - 1);
             break;
         case SDL_MOUSEBUTTONUP:
+#ifdef __IOS__
+            if (event.button.which == SDL_TOUCH_MOUSEID) {
+                break;
+            }
+#endif
             OnMouseButtonUp(event.button.button - 1);
             break;
         case SDL_MOUSEWHEEL:
@@ -604,6 +650,35 @@ void GfxWindowBackendSDL2::HandleSingleEvent(SDL_Event& event) {
         case SDL_DROPFILE:
             Ship::Context::GetInstance()->GetFileDropMgr()->SetDroppedFile(event.drop.file);
             break;
+#ifdef __IOS__
+        case SDL_APP_WILLENTERBACKGROUND:
+            mIsBackgrounded = true;
+            SaveIOSConfiguration();
+            SetIOSAudioPaused(true);
+            break;
+        case SDL_APP_DIDENTERBACKGROUND:
+        case SDL_APP_WILLENTERFOREGROUND:
+            break;
+        case SDL_APP_DIDENTERFOREGROUND:
+            if (Ship::Context::GetInstance()->GetControlDeck() != nullptr) {
+                SPDLOG_INFO("Controller reconciliation: reason=foreground");
+                Ship::Context::GetInstance()
+                    ->GetControlDeck()
+                    ->GetConnectedPhysicalDeviceManager()
+                    ->RefreshConnectedSDLGamepads("foreground");
+            }
+            SetIOSAudioPaused(false);
+            mIsBackgrounded = false;
+            break;
+        case SDL_APP_LOWMEMORY:
+            SPDLOG_WARN("iOS reported low memory");
+            break;
+        case SDL_APP_TERMINATING:
+            mIsBackgrounded = true;
+            SaveIOSConfiguration();
+            SetIOSAudioPaused(true);
+            break;
+#endif
         case SDL_QUIT:
             Close();
             break;
@@ -613,15 +688,15 @@ void GfxWindowBackendSDL2::HandleSingleEvent(SDL_Event& event) {
 void GfxWindowBackendSDL2::HandleEvents() {
     SDL_Event event;
     SDL_PumpEvents();
     while (SDL_PeepEvents(&event, 1, SDL_GETEVENT, SDL_FIRSTEVENT, SDL_CONTROLLERDEVICEADDED - 1) > 0) {
         HandleSingleEvent(event);
     }
-    while (SDL_PeepEvents(&event, 1, SDL_GETEVENT, SDL_CONTROLLERDEVICEREMOVED + 1, SDL_LASTEVENT) > 0) {
+    while (SDL_PeepEvents(&event, 1, SDL_GETEVENT, SDL_CONTROLLERDEVICEREMAPPED + 1, SDL_LASTEVENT) > 0) {
         HandleSingleEvent(event);
     }

     // resync fullscreen state
-#ifdef __APPLE__
+#if defined(__APPLE__) && !defined(__IOS__)
     auto nextFullscreenState = isNativeMacOSFullscreenActive(mWnd);
     if (mFullScreen != nextFullscreenState) {
         mFullScreen = nextFullscreenState;
@@ -633,7 +708,11 @@ void GfxWindowBackendSDL2::HandleEvents() {
 }

 bool GfxWindowBackendSDL2::IsFrameReady() {
+#ifdef __IOS__
+    return !mIsBackgrounded;
+#else
     return true;
+#endif
 }

 static uint64_t qpc_to_100ns(uint64_t qpc) {
diff --git a/src/libultraship/bridge/windowbridge.cpp b/src/libultraship/bridge/windowbridge.cpp
index bfa5e7ea..1e3ba376 100644
--- a/src/libultraship/bridge/windowbridge.cpp
+++ b/src/libultraship/bridge/windowbridge.cpp
@@ -2,6 +2,9 @@
 #include "ship/window/Window.h"
 #include "ship/Context.h"

+#include <chrono>
+#include <thread>
+
 extern "C" {

 uint32_t WindowGetWidth() {
@@ -20,6 +23,15 @@ bool WindowIsRunning() {
     return Ship::Context::GetInstance()->GetWindow()->IsRunning();
 }

+void WindowWaitForFrameReady() {
+    auto window = Ship::Context::GetInstance()->GetWindow();
+
+    while (window->IsRunning() && !window->IsFrameReady()) {
+        window->HandleEvents();
+        std::this_thread::sleep_for(std::chrono::milliseconds(16));
+    }
+}
+
 int32_t WindowGetPosX() {
     return Ship::Context::GetInstance()->GetWindow()->GetPosX();
 }
diff --git a/src/libultraship/controller/controldevice/controller/mapping/ControllerDefaultMappings.cpp b/src/libultraship/controller/controldevice/controller/mapping/ControllerDefaultMappings.cpp
index c431c346..1bce6004 100644
--- a/src/libultraship/controller/controldevice/controller/mapping/ControllerDefaultMappings.cpp
+++ b/src/libultraship/controller/controldevice/controller/mapping/ControllerDefaultMappings.cpp
@@ -51,7 +51,7 @@ void ControllerDefaultMappings::SetDefaultKeyboardKeyToButtonMappings(
         { BTN_L, { Ship::KbScancode::LUS_KB_E } },
         { BTN_R, { Ship::KbScancode::LUS_KB_R } },
         { BTN_Z, { Ship::KbScancode::LUS_KB_Z } },
-        { BTN_START, { Ship::KbScancode::LUS_KB_SPACE } },
+        { BTN_START, { Ship::KbScancode::LUS_KB_SPACE, Ship::KbScancode::LUS_KB_ENTER } },
         { BTN_CUP, { Ship::KbScancode::LUS_KB_ARROWKEY_UP } },
         { BTN_CDOWN, { Ship::KbScancode::LUS_KB_ARROWKEY_DOWN } },
         { BTN_CLEFT, { Ship::KbScancode::LUS_KB_ARROWKEY_LEFT } },
diff --git a/src/ship/Context.cpp b/src/ship/Context.cpp
index 0c36b4f4..7778d370 100644
--- a/src/ship/Context.cpp
+++ b/src/ship/Context.cpp
@@ -398,11 +398,6 @@ std::string Context::GetAppBundlePath() {
     }
 #endif

-#ifdef __IOS__
-    const char* home = getenv("HOME");
-    return std::string(home) + "/Documents";
-#endif
-
 #ifdef NON_PORTABLE
     return CMAKE_INSTALL_PREFIX;
 #else
diff --git a/src/ship/audio/Audio.cpp b/src/ship/audio/Audio.cpp
index 6efce2c2..b715a25f 100644
--- a/src/ship/audio/Audio.cpp
+++ b/src/ship/audio/Audio.cpp
@@ -62,4 +62,10 @@ std::shared_ptr<std::vector<AudioBackend>> Audio::GetAvailableAudioBackends() {
     return mAvailableAudioBackends;
 }

+void Audio::SetPaused(bool paused) {
+    if (mAudioPlayer != nullptr) {
+        mAudioPlayer->SetPaused(paused);
+    }
+}
+
 } // namespace Ship
diff --git a/src/ship/audio/SDLAudioPlayer.cpp b/src/ship/audio/SDLAudioPlayer.cpp
index c74c8840..081e678e 100644
--- a/src/ship/audio/SDLAudioPlayer.cpp
+++ b/src/ship/audio/SDLAudioPlayer.cpp
@@ -9,6 +9,9 @@ SDLAudioPlayer::~SDLAudioPlayer() {
 }

 bool SDLAudioPlayer::DoInit() {
+#ifdef __IOS__
+    SDL_SetHint(SDL_HINT_AUDIO_CATEGORY, "playback");
+#endif
     if (SDL_Init(SDL_INIT_AUDIO) != 0) {
         SPDLOG_ERROR("SDL init error: %s\n", SDL_GetError());
         return false;
@@ -35,9 +38,20 @@ int SDLAudioPlayer::Buffered() {
 }

 void SDLAudioPlayer::Play(const uint8_t* buf, size_t len) {
-    if (Buffered() < 6000) {
+    if (!mPaused && Buffered() < 6000) {
         // Don't fill the audio buffer too much in case this happens
         SDL_QueueAudio(mDevice, buf, len);
     }
 }
+
+void SDLAudioPlayer::SetPaused(bool paused) {
+    mPaused = paused;
+    if (mDevice == 0) {
+        return;
+    }
+    SDL_PauseAudioDevice(mDevice, paused ? 1 : 0);
+    if (paused) {
+        SDL_ClearQueuedAudio(mDevice);
+    }
+}
 } // namespace Ship
diff --git a/src/ship/port/mobile/MobileImpl.cpp b/src/ship/port/mobile/MobileImpl.cpp
index 34391d1b..8d73c9cb 100644
--- a/src/ship/port/mobile/MobileImpl.cpp
+++ b/src/ship/port/mobile/MobileImpl.cpp
@@ -1,7 +1,6 @@
 #if defined(__ANDROID__) || defined(__IOS__)
-#include "MobileImpl.h"
+#include "ship/port/mobile/MobileImpl.h"
 #include <SDL2/SDL.h>
-#include "ship/public/bridge/consolevariablebridge.h"

 #include <imgui_internal.h>

diff --git a/src/ship/window/gui/Gui.cpp b/src/ship/window/gui/Gui.cpp
index 17f0c2b0..9c39077a 100644
--- a/src/ship/window/gui/Gui.cpp
+++ b/src/ship/window/gui/Gui.cpp
@@ -7,6 +7,10 @@
 #include <string>
 #include <vector>

+#ifdef __IOS__
+extern "C" void MaskPad_SetTouchControlsMenuVisible(int visible);
+#endif
+
 #include "ship/config/Config.h"
 #include "ship/Context.h"
 #include "ship/config/ConsoleVariable.h"
@@ -31,7 +35,7 @@
 #endif

 #if defined(__ANDROID__) || defined(__IOS__)
-#include "port/mobile/MobileImpl.h"
+#include "ship/port/mobile/MobileImpl.h"
 #endif

 #ifdef ENABLE_OPENGL
@@ -583,6 +587,10 @@ void Gui::DrawMenu() {
         GetMenu()->Draw();
     }

+#ifdef __IOS__
+    MaskPad_SetTouchControlsMenuVisible(GetMenuOrMenubarVisible());
+#endif
+
     for (auto& windowIter : mGuiWindows) {
         windowIter.second->Update();
         windowIter.second->Draw();
diff --git a/include/ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.h b/include/ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.h
index 89c05b55..8da223bf 100644
--- a/include/ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.h
+++ b/include/ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.h
@@ -4,6 +4,7 @@
 #include <unordered_set>
 #include <vector>
 #include <string>
+#include <cstdint>
 #include <SDL2/SDL.h>
 
 namespace Ship {
@@ -22,9 +23,15 @@ class ConnectedPhysicalDeviceManager {
 
     void HandlePhysicalDeviceConnect(int32_t sdlDeviceIndex);
     void HandlePhysicalDeviceDisconnect(int32_t sdlJoystickInstanceId);
-    void RefreshConnectedSDLGamepads();
+    void HandlePhysicalDeviceRemap(int32_t sdlJoystickInstanceId);
+    void RefreshConnectedSDLGamepads(const char* reason = "manual");
 
   private:
+    static constexpr uint8_t kPortCount = 4;
+
+    bool GamepadHandleIsCurrent(int32_t instanceId, SDL_GameController* gamepad) const;
+    void CloseGamepad(int32_t instanceId, SDL_GameController* gamepad, const char* reason);
+    void AssignNewGamepadToFirstFreePort(int32_t instanceId);
     std::unordered_map<int32_t, SDL_GameController*> mConnectedSDLGamepads;
     std::unordered_map<int32_t, std::string> mConnectedSDLGamepadNames;
     std::unordered_map<uint8_t, std::unordered_set<int32_t>> mIgnoredInstanceIds;
diff --git a/include/ship/controller/physicaldevice/SDLAddRemoveDeviceEventHandler.h b/include/ship/controller/physicaldevice/SDLAddRemoveDeviceEventHandler.h
index 63c3afa8..6aadf725 100644
--- a/include/ship/controller/physicaldevice/SDLAddRemoveDeviceEventHandler.h
+++ b/include/ship/controller/physicaldevice/SDLAddRemoveDeviceEventHandler.h
@@ -1,6 +1,7 @@
 #pragma once
 
 #include "ship/window/gui/GuiWindow.h"
+#include <cstdint>
 
 namespace Ship {
 
@@ -13,5 +14,8 @@ class SDLAddRemoveDeviceEventHandler : public GuiWindow {
     void InitElement() override;
     void DrawElement() override;
     void UpdateElement() override;
+
+  private:
+    uint32_t mLastReconcileTicks = 0;
 };
 } // namespace Ship
diff --git a/src/ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.cpp b/src/ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.cpp
index 7f0397e2..bed82a70 100644
--- a/src/ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.cpp
+++ b/src/ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.cpp
@@ -1,11 +1,16 @@
 #include "ship/controller/physicaldevice/ConnectedPhysicalDeviceManager.h"
 #include <spdlog/spdlog.h>
+#include <vector>
 
 namespace Ship {
 ConnectedPhysicalDeviceManager::ConnectedPhysicalDeviceManager() {
+    RefreshConnectedSDLGamepads("startup");
 }
 
 ConnectedPhysicalDeviceManager::~ConnectedPhysicalDeviceManager() {
+    for (const auto& [instanceId, gamepad] : mConnectedSDLGamepads) {
+        CloseGamepad(instanceId, gamepad, "shutdown");
+    }
 }
 
 std::unordered_map<int32_t, SDL_GameController*>
@@ -13,7 +18,7 @@ ConnectedPhysicalDeviceManager::GetConnectedSDLGamepadsForPort(uint8_t portIndex
     std::unordered_map<int32_t, SDL_GameController*> result;
 
     for (const auto& [instanceId, gamepad] : mConnectedSDLGamepads) {
-        if (!PortIsIgnoringInstanceId(portIndex, instanceId)) {
+        if (!PortIsIgnoringInstanceId(portIndex, instanceId) && GamepadHandleIsCurrent(instanceId, gamepad)) {
             result[instanceId] = gamepad;
         }
     }
@@ -42,20 +47,74 @@ void ConnectedPhysicalDeviceManager::UnignoreInstanceIdForPort(uint8_t portIndex
 }
 
 void ConnectedPhysicalDeviceManager::HandlePhysicalDeviceConnect(int32_t sdlDeviceIndex) {
-    RefreshConnectedSDLGamepads();
+    SPDLOG_DEBUG("Controller reconciliation event: add device index {}", sdlDeviceIndex);
+    RefreshConnectedSDLGamepads("device-added");
 }
 
 void ConnectedPhysicalDeviceManager::HandlePhysicalDeviceDisconnect(int32_t sdlJoystickInstanceId) {
-    RefreshConnectedSDLGamepads();
+    SPDLOG_DEBUG("Controller reconciliation event: remove instance {}", sdlJoystickInstanceId);
+    RefreshConnectedSDLGamepads("device-removed");
+}
+
+void ConnectedPhysicalDeviceManager::HandlePhysicalDeviceRemap(int32_t sdlJoystickInstanceId) {
+    SPDLOG_DEBUG("Controller reconciliation event: remap instance {}", sdlJoystickInstanceId);
+    RefreshConnectedSDLGamepads("device-remapped");
+}
+
+bool ConnectedPhysicalDeviceManager::GamepadHandleIsCurrent(int32_t instanceId,
+                                                            SDL_GameController* gamepad) const {
+    if (gamepad == nullptr || SDL_GameControllerGetAttached(gamepad) != SDL_TRUE) {
+        return false;
+    }
+
+    SDL_Joystick* joystick = SDL_GameControllerGetJoystick(gamepad);
+    return joystick != nullptr && SDL_JoystickInstanceID(joystick) == instanceId;
+}
+
+void ConnectedPhysicalDeviceManager::CloseGamepad(int32_t instanceId, SDL_GameController* gamepad,
+                                                   const char* reason) {
+    const auto name = mConnectedSDLGamepadNames.find(instanceId);
+    SPDLOG_INFO("Controller release: reason={} instance={} name={} close={}", reason, instanceId,
+                name == mConnectedSDLGamepadNames.end() ? "unknown" : name->second, gamepad != nullptr);
+    if (gamepad != nullptr) {
+        SDL_GameControllerClose(gamepad);
+    }
+}
+
+void ConnectedPhysicalDeviceManager::AssignNewGamepadToFirstFreePort(int32_t instanceId) {
+    for (uint8_t port = 0; port < kPortCount; port++) {
+        mIgnoredInstanceIds[port].insert(instanceId);
+    }
+
+    for (uint8_t port = 0; port < kPortCount; port++) {
+        bool occupied = false;
+        for (const auto& [otherInstanceId, gamepad] : mConnectedSDLGamepads) {
+            if (otherInstanceId != instanceId && !PortIsIgnoringInstanceId(port, otherInstanceId) &&
+                GamepadHandleIsCurrent(otherInstanceId, gamepad)) {
+                occupied = true;
+                break;
+            }
+        }
+        if (!occupied) {
+            mIgnoredInstanceIds[port].erase(instanceId);
+            SPDLOG_INFO("Controller assignment: instance={} player-slot={}", instanceId, port + 1);
+            return;
+        }
+    }
+
+    SPDLOG_WARN("Controller assignment: instance={} has no free player slot", instanceId);
 }
 
-void ConnectedPhysicalDeviceManager::RefreshConnectedSDLGamepads() {
-    mConnectedSDLGamepads.clear();
-    mConnectedSDLGamepadNames.clear();
+void ConnectedPhysicalDeviceManager::RefreshConnectedSDLGamepads(const char* reason) {
     static SDL_JoystickGUID sZeroGuid;
+    struct EnumeratedGamepad {
+        int32_t deviceIndex;
+        int32_t instanceId;
+        std::string guid;
+    };
+    std::vector<EnumeratedGamepad> enumeratedGamepads;
 
     for (int32_t i = 0; i < SDL_NumJoysticks(); i++) {
-
         SDL_JoystickGUID deviceGUID = SDL_JoystickGetDeviceGUID(i);
         if (SDL_memcmp(&deviceGUID, &sZeroGuid, sizeof(deviceGUID)) == 0) {
             SPDLOG_WARN(
@@ -69,7 +128,7 @@ void ConnectedPhysicalDeviceManager::RefreshConnectedSDLGamepads() {
         SDL_JoystickGetGUIDString(deviceGUID, deviceGuidCStr, sizeof(deviceGuidCStr));
 
         if (!SDL_IsGameController(i)) {
-            SPDLOG_WARN("SDL Joystick (GUID: {}) not recognized as gamepad."
+            SPDLOG_DEBUG("SDL Joystick (GUID: {}) not recognized as gamepad."
                         "This is likely due to a missing mapping string in gamecontrollerdb.txt."
                         "Refer to https://github.com/mdqinc/SDL_GameControllerDB for more information.",
                         deviceGuidCStr);
@@ -76,22 +135,76 @@ void ConnectedPhysicalDeviceManager::RefreshConnectedSDLGamepads() {
             continue;
         }
 
-        auto gamepad = SDL_GameControllerOpen(i);
+        const auto instanceId = SDL_JoystickGetDeviceInstanceID(i);
+        if (instanceId < 0) {
+            SPDLOG_ERROR("SDL JoystickGetDeviceInstanceID error (GUID: {} index: {}): {}", deviceGuidCStr, i,
+                         SDL_GetError());
+            continue;
+        }
+        enumeratedGamepads.push_back({ i, instanceId, deviceGuidCStr });
+    }
+
+    SPDLOG_DEBUG("Controller reconciliation: reason={} enumerated={} tracked={}", reason, enumeratedGamepads.size(),
+                 mConnectedSDLGamepads.size());
+
+    std::unordered_set<int32_t> currentInstanceIds;
+    for (const auto& device : enumeratedGamepads) {
+        currentInstanceIds.insert(device.instanceId);
+    }
+
+    std::vector<int32_t> staleInstanceIds;
+    std::unordered_set<int32_t> reopenWithExistingAssignment;
+    for (const auto& [instanceId, gamepad] : mConnectedSDLGamepads) {
+        if (!currentInstanceIds.contains(instanceId) || !GamepadHandleIsCurrent(instanceId, gamepad)) {
+            CloseGamepad(instanceId, gamepad, reason);
+            staleInstanceIds.push_back(instanceId);
+            if (currentInstanceIds.contains(instanceId)) {
+                reopenWithExistingAssignment.insert(instanceId);
+            }
+        }
+    }
+    for (const auto instanceId : staleInstanceIds) {
+        const bool stillEnumerated = currentInstanceIds.contains(instanceId);
+        mConnectedSDLGamepads.erase(instanceId);
+        mConnectedSDLGamepadNames.erase(instanceId);
+        if (!stillEnumerated) {
+            for (uint8_t port = 0; port < kPortCount; port++) {
+                mIgnoredInstanceIds[port].erase(instanceId);
+            }
+        }
+    }
+
+    for (const auto& device : enumeratedGamepads) {
+        const auto existing = mConnectedSDLGamepads.find(device.instanceId);
+        if (existing != mConnectedSDLGamepads.end()) {
+            const char* name = SDL_GameControllerName(existing->second);
+            if (name != nullptr) {
+                mConnectedSDLGamepadNames[device.instanceId] = name;
+            }
+            continue;
+        }
+
+        auto gamepad = SDL_GameControllerOpen(device.deviceIndex);
         if (gamepad == nullptr) {
-            SPDLOG_ERROR("SDL GameControllerOpen error (GUID: {}): {}", deviceGuidCStr, SDL_GetError());
+            SPDLOG_ERROR("SDL GameControllerOpen error (GUID: {} index: {}): {}", device.guid, device.deviceIndex,
+                         SDL_GetError());
             continue;
         }
 
         auto instanceId = SDL_JoystickInstanceID(SDL_GameControllerGetJoystick(gamepad));
-        if (instanceId < 0) {
-            SPDLOG_ERROR("SDL JoystickInstanceID error (GUID: {}): {}", deviceGuidCStr, SDL_GetError());
+        if (instanceId < 0 || instanceId != device.instanceId ||
+            SDL_GameControllerGetAttached(gamepad) != SDL_TRUE) {
+            SPDLOG_ERROR("SDL controller identity/attachment error (GUID: {} index: {} expected-instance: {} "
+                         "actual-instance: {}): {}",
+                         device.guid, device.deviceIndex, device.instanceId, instanceId, SDL_GetError());
+            SDL_GameControllerClose(gamepad);
             continue;
         }
 
         std::string gamepadName;
         auto name = SDL_GameControllerName(gamepad);
         if (name == nullptr) {
-            gamepadName = deviceGuidCStr;
+            gamepadName = device.guid;
             SPDLOG_WARN("SDL_GameControllerName returned null. Setting name to GUID \"{}\" instead.", gamepadName);
         } else {
             gamepadName = name;
@@ -99,10 +212,11 @@ void ConnectedPhysicalDeviceManager::RefreshConnectedSDLGamepads() {
 
         mConnectedSDLGamepads[instanceId] = gamepad;
         mConnectedSDLGamepadNames[instanceId] = gamepadName;
-
-        for (uint8_t port = 1; port < 4; port++) {
-            mIgnoredInstanceIds[port].insert(instanceId);
+        if (!reopenWithExistingAssignment.contains(instanceId)) {
+            AssignNewGamepadToFirstFreePort(instanceId);
         }
+        SPDLOG_INFO("Controller open: reason={} device-index={} instance={} name={}", reason, device.deviceIndex,
+                    instanceId, gamepadName);
     }
 }
 } // namespace Ship
diff --git a/src/ship/controller/physicaldevice/SDLAddRemoveDeviceEventHandler.cpp b/src/ship/controller/physicaldevice/SDLAddRemoveDeviceEventHandler.cpp
index 2e519f81..e0e45b10 100644
--- a/src/ship/controller/physicaldevice/SDLAddRemoveDeviceEventHandler.cpp
+++ b/src/ship/controller/physicaldevice/SDLAddRemoveDeviceEventHandler.cpp
@@ -9,6 +9,7 @@ SDLAddRemoveDeviceEventHandler::~SDLAddRemoveDeviceEventHandler() {
 }
 
 void SDLAddRemoveDeviceEventHandler::InitElement() {
+    mLastReconcileTicks = SDL_GetTicks();
 }
 
 void SDLAddRemoveDeviceEventHandler::DrawElement() {
@@ -30,5 +31,17 @@ void SDLAddRemoveDeviceEventHandler::UpdateElement() {
         Context::GetInstance()->GetControlDeck()->GetConnectedPhysicalDeviceManager()->HandlePhysicalDeviceDisconnect(
             event.cdevice.which);
     }
+
+    while (SDL_PeepEvents(&event, 1, SDL_GETEVENT, SDL_CONTROLLERDEVICEREMAPPED, SDL_CONTROLLERDEVICEREMAPPED) > 0) {
+        Context::GetInstance()->GetControlDeck()->GetConnectedPhysicalDeviceManager()->HandlePhysicalDeviceRemap(
+            event.cdevice.which);
+    }
+
+    const uint32_t now = SDL_GetTicks();
+    if (now - mLastReconcileTicks >= 1000) {
+        Context::GetInstance()->GetControlDeck()->GetConnectedPhysicalDeviceManager()->RefreshConnectedSDLGamepads(
+            "active-check");
+        mLastReconcileTicks = now;
+    }
 }
 } // namespace Ship
